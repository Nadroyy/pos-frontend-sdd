import { useState } from 'react';
type Props={onScan:(barcode:string)=>boolean};
export function BarcodeInput({onScan}:Props){const [barcode,setBarcode]=useState(''); const [error,setError]=useState(''); function submit(){const ok=onScan(barcode); if(ok){setBarcode('');setError('');}else{setError('Barcode not found.');}} return <section className='panel'><h2>Manual Barcode</h2><label>Barcode<input value={barcode} onChange={e=>setBarcode(e.target.value)} onKeyDown={e=>e.key==='Enter'&&submit()} placeholder='Example: 200001'/></label><button onClick={submit}>Scan / Add</button>{error&&<p className='error'>{error}</p>}</section>;}
