import { useState } from 'react';
type Props={onScan:(barcode:string)=>boolean};
export function BarcodeInput({onScan}:Props){const [barcode,setBarcode]=useState(''); const [error,setError]=useState(''); function submit(){const ok=onScan(barcode); if(ok){setBarcode('');setError('');}else{setError('Código de barras no encontrado.');}} return <section className='panel'><h2>Código de Barras Manual</h2><label>Código de barras<input value={barcode} onChange={e=>setBarcode(e.target.value)} onKeyDown={e=>e.key==='Enter'&&submit()} placeholder='Ejemplo: 200001'/></label><button onClick={submit}>Escanear / Agregar</button>{error&&<p className='error'>{error}</p>}</section>;}
