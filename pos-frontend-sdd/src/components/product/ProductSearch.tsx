import type { Product } from '../../types/product';
import { formatMoney } from '../../utils/money';
type Props={products:Product[];searchTerm:string;category:string;categories:string[];onSearchChange:(v:string)=>void;onCategoryChange:(v:string)=>void;onAdd:(p:Product)=>void};
export function ProductSearch({products,searchTerm,category,categories,onSearchChange,onCategoryChange,onAdd}:Props){return <section className='panel'><h2>Búsqueda de productos</h2><div className='grid two'><label>Buscar por nombre<input value={searchTerm} onChange={e=>onSearchChange(e.target.value)} placeholder='Leche, pan, jabón...' /></label><label>Categoría<select value={category} onChange={e=>onCategoryChange(e.target.value)}>{categories.map(c=><option key={c}>{c}</option>)}</select></label></div><div>{products.length===0?<p className='muted'>No se encontraron productos.</p>:products.map(p=><article className='product-card' key={p.id}><div><strong>{p.name}</strong><p>{p.category} - Código {p.barcode}</p><p>Existencias: {p.stock}</p></div><div className='right'><strong>{formatMoney(p.price)}</strong><button onClick={()=>onAdd(p)}>Agregar</button></div></article>)}</div></section>;}
